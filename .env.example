# Supabase Configuration
# SECURITY NOTE: These values will be exposed in the frontend bundle
# Make sure to enable Row Level Security (RLS) in your Supabase database

# Your Supabase project URL (safe to expose)
VITE_SUPABASE_URL=your_supabase_project_url_here

# Your Supabase anon/public key (designed for frontend use)
# This key requires proper RLS policies for security
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend API Configuration
# Your Railway deployed backend API URL
VITE_API_BASE_URL=https://your-app-name.up.railway.app

# NEVER include your service role key in frontend code!
# Service role key should only be used server-side
